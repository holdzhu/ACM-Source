#include <cstdio>
#include <cstring>

char s[101];

int main()
{
	int T;
	scanf("%d", &T);
	while (T--)
	{
		scanf("%s", s);
		int l = strlen(s);
		for (int i = 0; i < 3; ++i)
		{
			for (int j = 0; j <= l; ++j)
			{
				
			}
		}
	}
	return 0;
}
