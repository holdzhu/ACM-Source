#include <iostream>
#include <set>

using namespace std;

int main()
{
	int n, m;
	cin >> n >> m;
	set<string> s;
	for (int i = 0; i < n; ++i)
	{
		string t;
		cin >> t;
		for (int i = 0; i < t.length(); ++i)
		{
			int c = t[i];
			for (int i = 'a'; i < 'd'; ++i)
			{
				
			}
		}
	}
	return 0;
}
