#include <cstdio>

const int maxnode = 1e6 + 10;
const int sigma = 26;

char s[10001];
int ch[maxnode][sigma];
bool val[maxnode];
int sz = 1;
char w[maxnode];

int main()
{
	int n;
	scanf("%d %s", &n, s);
	int m;
	scanf("%d", &m);
	for (int i = 0; i < m; ++i)
	{
		scanf("%s", w);
		int u = 0;
		for (int j = 0; w[j]; ++j)
		{
			if (w[j] )
		}
	}
	return 0;
}
