#include <cstdio>
#include <queue>
#include <set>

using namespace std;

set<int> c[26];

int main()
{
	int n, m, k;
	scanf("%d %d %d", &n, &m, &k);
	char s[n][m + 1];
	for (int i = 0; i < n; ++i)
	{
		scanf("%s", s[n]);
	}
	
	return 0;
}
