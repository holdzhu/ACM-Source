#include <cstdio>
#include <climits>
#include <algorithm>

using namespace std;

int w[10000];
long long dp[]

long long dfs(int r, int m)
{
	long long rnt = LONG_LONG_MAX;

}

int main()
{
	int n, m;
	scanf("%d %d", &n, &m);
	for (int i = 0; i < n; ++i)
	{
		scanf("%d", &w[i]);
	}
	printf("%lld\n", dfs(n, m));
	return 0;
}
